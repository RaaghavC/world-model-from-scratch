# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Large / regenerable dataset (obs.npy is ~600 MB — exceeds GitHub's file limit).
# Everything in data/ is regenerated by `bash run_all.sh` (the collect step).
data/

# Logs
artifacts/*.log

# OS cruft
.DS_Store
